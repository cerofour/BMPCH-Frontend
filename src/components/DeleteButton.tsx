import { Button } from "react-bootstrap";

export default function MyDeleteButton() {
    return(
        <>
            <Button variant="outline-danger" style={{ fontWeight: 'bold' }}>X</Button>{' '}
        </>
    );
}